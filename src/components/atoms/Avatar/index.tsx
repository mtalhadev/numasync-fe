// import { Avatar } from '@nextui-org/react';
// import { AvatarTypes } from 'src/types/avatarTypes';

// const CustomAvatar = ({ Icon, classNames, IconSize }: AvatarTypes) => {
//   return (
//     <Avatar
//       icon={<Icon size={IconSize || '20'} />}
//       classNames={{
//         base: `${classNames?.icon} bg-defaulr-50 !size-10 border-default-100 cursor-pointer hidden sm:flex`,
//         icon: `${classNames?.icon} text-default-500`,
//       }}
//     />
//   );
// };

// export default CustomAvatar;
